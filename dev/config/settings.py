"""控制端参数配置。

约定：
- **所有阈值、限幅、坐标目标都集中在本文件**，不要在业务代码里散落硬编码；
- 标注"现场可调"的参数在标定后请把实测值写回这里，并注明日期；
- 单位统一见 AGENTS.md §5.2（像素/角度/百分比）。

调试阶段默认把电调最大脉宽限制在 1600us（约 20% 行程），避免小车速度过快。
注意：1500us 停、**≈1545us 才起转**（架空实测），所以调试脉宽的可用下限是 1545 而不是 1500。
比赛前再调整为 full speed。
"""
from __future__ import annotations

# ---------------------------------------------------------------- 图像坐标
IMG_W = 640
IMG_H = 480

# ---------------------------------------------------------------- 电调/电机
# ⚠️ 脉宽语义（2026-09-16 架空实测）：
#   1500us = 停；**1540us 轮子仍不转，≈1545us 才起转** → 死区约 45us。
#   电调特性会随电池电压/上电时序漂移，所以任何"要让车动"的脉宽都取在死区之上留余量，
#   低于 ESC_DEADBAND_US 的值只会让车原地不动（曾因此浪费一次台架）。
ESC_STOP_US = 1500      # 停止/中位脉宽
ESC_DEADBAND_US = 1545  # ★ 起转脉宽（死区上沿）：低于它电调无输出、车不动【现场可调】
ESC_CREEP_US = 1560     # 蠕动/起步对齐脉宽（死区 + 15us，最慢可靠速度）
ESC_DEBUG_MAX_US = 1600 # 调试阶段最大脉宽（≈20% 行程的安全限速）
ESC_MAX_US = 2000       # 比赛/全速最大脉宽
ESC_MIN_US = 1400       # 最大倒车脉宽

# ---------------------------------------------------------------- 舵机
SERVO_CENTER_ANGLE = 90
SERVO_MIN_ANGLE = 60
SERVO_MAX_ANGLE = 120

# ---------------------------------------------------------------- 视觉/控制
TARGET_X = 320.0        # 期望车道中心像素值（摄像头偏装时现场标定）
UDP_PORT = 5000

# 转向限速：每秒钟最多改变多少度（防止从 0 直接跳满舵）
# 2026-09-16：240°/s 在实车上偏猛（一帧就能打到限幅），降到 120°/s 更稳
STEERING_SLEW_DEG_PER_S = 120.0

# ---------------------------------------------------------------- 可靠性
FAILSAFE_TIMEOUT = 2.0      # 连续多久没收到 UDP 就安全停车（秒）
ZEBRA_STOP_SECONDS = 10.0   # 斑马线停车时长（规则：10s，不足会被罚时）

# 状态切换防抖帧数
ZEBRA_HYSTERESIS_FRAMES = 3
CONE_HYSTERESIS_FRAMES = 3
SIGN_HYSTERESIS_FRAMES = 3
TRAFFIC_LIGHT_HYSTERESIS_FRAMES = 5     # 绿灯要连续 5 帧（禁止单帧决策）

# ---------------------------------------------------------------- 日志/调试
PRINT_EVERY_N = 10                      # 视觉进程每 N 帧打印一行状态
STATUS_FILE = "/tmp/smartcar_status.json"   # 车端状态文件（本地测试可覆盖）

# ---------------------------------------------------------------- 发车遮挡检测
# §3.1.7：挡板被移开 = 发车信号。必须边沿触发（先见板 → 再连续多帧不见板）。
# 2026-09-16 加固：两路判据（最大蓝色连通域面积 + 蓝色主导度），覆盖"板先放好"与"启动后才放板"两种时序。
START_GATE_ROI = (0.15, 0.85, 0.20, 0.95)   # (x0, x1, y0, y1) 比例，取画面中下部
START_BLUE_HSV_LOW = (95, 80, 60)           # 蓝色挡板 HSV 下限【现场可调】
START_BLUE_HSV_HIGH = (135, 255, 255)       # 蓝色挡板 HSV 上限【现场可调】
START_BLUE_AREA_THRESH = 0.06               # 判据①：最大蓝色连通域面积 / ROI 面积【现场可调】
START_BLUE_AREA_THRESH_LOW = 0.03           # 判据②（主导度）成立时的面积放宽线
START_BLUE_DOMINANCE_THRESH = 35.0          # 判据②：最蓝 10% 像素的 mean(clip(B-max(R,G),0,255))【现场可调】
START_USE_DETAIL = False                    # 判据③：近距离"画面细节骤降"（默认关，贴脸场景才需要）
START_DETAIL_DROP_RATIO = 0.45              # 判据③：相对基线下降多少算贴脸
START_REPORT_EVERY_S = 5.0                  # 未见板时每隔多久打印一次当前读数（供操作员定位问题）
START_ARM_FRAMES = 3                        # 连续 N 帧判定"有遮挡"才武装
START_RELEASE_FRAMES = 5                    # 武装后连续 N 帧"无遮挡"才发车（去抖）
START_TIMEOUT_S = 90.0                      # 超时仅告警（不自动发车，保守）
START_USE_EDGE_DENSITY = False              # 是否同时要求边缘密度判据（备选，默认关）
START_EDGE_DENSITY_THRESH = 0.02            # 边缘密度阈值（START_USE_EDGE_DENSITY=True 时生效）

# ---------------------------------------------------------------- 扫线
LANE_ROI_TOP_RATIO = 0.45       # ROI 上边界占画面高度比例
LANE_ROI_BOTTOM_MARGIN = 16     # 距画面底部保留的像素（避免拍到车头）
LANE_ROW_STEP = 2               # 每隔多少行扫一次
LANE_WEIGHT_PEAK = 8.0          # 纵向权重峰值（中间行权重最大，对应"看得不远不近"）
LANE_WHITE_S_MAX = 70           # 白线饱和度上限（HSV）
LANE_WHITE_V_MIN = 120          # 白线亮度绝对下限（防纯黑画面产生噪声）
# 亮度阈值 = 中位数 + SPLIT × (95分位 − 中位数)，即**在"地面底色"与"更亮的线"之间自动分开**
# 2026-09-16 实测教训：原公式 max(V_MIN, 0.75×V95) 在地板(141)与打印白线(169)只差 28 级时
# 算出 127 → 整片地板都被判成"白"（白掩膜占 21% 像素）→ 扫线锁到地板，中心乱跳。
LANE_WHITE_V_SPLIT = 0.5
LANE_MAX_ERROR_PX = 160         # 误差限幅（像素）
LANE_ERROR_SCALE = 4.0          # 误差归一化除数（沿官方惯例，便于复用其 PID 起点）
LANE_MIN_VALID_ROWS = 5         # 双侧齐全的行少于这个数 → 置信度直接判 0（不输出乱跳的中心）
LANE_EDGE_MARGIN = 8            # 距画面左右边缘多少像素内的"白点"不算车道线（防画面边框/墙体伪影）
LANE_MAX_LINE_W_PX = 60         # 白线连通段最大宽度：更宽的是地面/纸边亮区，不是线【现场可调】
# 连通域形状过滤：只保留「细长」的连通域（车道线），去掉块状亮区（**打印图表面反光**是最大来源）。
# 判据用**尺度无关的细长比**：长度 = bbox 长边；厚度 = 面积/长度；细长比 = 长度/厚度。
# ⚠️ 不要改回"厚度 ≤ N 像素"的绝对阈值：前视浅角度下白线的投影厚度本来就大，
#    绝对阈值会把真线一起滤掉（2026-09-16 因此把置信度从 0.6 打到 0.1、车完全不动）。
LANE_LINE_MIN_LEN_PX = 20       # 连通域最长边至少这么长才算「线」
LANE_LINE_MIN_ELONG = 3.0       # 细长比下限（块状反光约 1~2.5，白线通常 >5）【现场可调】
LANE_LINE_MAX_THICK_PX = 80     # 次级保险：只挡"整片亮区"，不要用它做精细判据

# 逐行连续跟踪（把"线"和"画面中间的成片反光"分开的**主要**手段）
LANE_TRACK_MAX_JUMP_PX = 12     # 相邻行之间边界最多移动多少像素（透视变化是渐变的）
LANE_TRACK_MAX_MISS = 6         # 允许连续多少行缺失（被光斑/遮挡打断）
LANE_TRACK_WIDTH_TOL = 0.6      # 段宽相对已跟踪宽度中位数的允许偏差（反光宽度不稳，会被这条拦下）
LANE_TRACK_MIN_LEN = 30         # 从底部起跟的路径短于它 → 改扫全部种子行、取最长路径
LANE_TRACK_SEED_STEP = 8        # 多种子扫描的取样步长（越大越快、越粗）
# 配对宽度闸门：车道是 1.22m，在画面里的投影宽度有物理上下界。
# 2026-09-19 加：多种子跟踪在"画面里没有真线"时也能凑出很长的路径（反光/斑马线），
# 所以要求"左右都在、且配对宽度落在这个区间"才算有效行——宁可不给中心，也不给错中心。
LANE_PAIR_W_MIN_PX = 110
LANE_PAIR_W_MAX_PX = 620

# 转向符号：+1 = 角度增大 → 右转；实车若相反就改成 -1（可写进 config/site.yaml，不必改代码）
STEER_SIGN = 1

# ---------------------------------------------------------------- 误差滤波
ERROR_FILTER_WINDOW = 5         # 滑动窗口长度
# 与窗口中位数偏差超过该值判为离群（单位是误差单位：1 单位 = 4px）
# 2026-09-16 实车教训：原来 50（=200px）太松，扫线偶发跳 150px 照样进 PID → 车左右猛打，
# 收紧到 15（≈60px）；60px ≈ 单帧 12° 偏航，正常行驶不可能出现
ERROR_FILTER_OUTLIER = 15.0

# ---------------------------------------------------------------- PID（巡线）
# 起点沿用官方上届巡线的"纯 PD"结构与经验比例（kp≈0.235 / kd≈0.6），
# 按我们的误差单位（px/4、限幅 ±40）与舵机角度限幅（±30°）等比换算。
# ⚠️ 这只是**起点**，必须现场整定；官方积分项恒为 0，我们默认也保持 0。
LANE_KP = 0.45
LANE_KI = 0.0
# 微分项对噪声最敏感：实车第一次跑（2026-09-16）就是被它把扫线跳变放大成满舵。
# 先用 0.2 跑稳，确认中心稳定后再往上加。
LANE_KD = 0.20
LANE_INTEGRAL_LIMIT = 20.0      # 积分限幅（误差单位·秒）
LANE_DERIV_ALPHA = 0.5          # 微分低通系数（0~1，越小越平滑）
LANE_STEER_LIMIT_DEG = 30.0     # 转向角度增量限幅

# ---------------------------------------------------------------- 仲裁层
ARBITER_CONF_THRESH = 0.35          # 扫线置信度阈值
ARBITER_LOW_CONF_FRAMES = 3         # 连续 N 帧低置信度才降级
ARBITER_HOLD_S = 0.8                # 降级后允许靠"上一帧有效值"维持多久
ARBITER_STOP_S = 1.5                # 超过该时长仍无有效信息 → 要求停车
ARBITER_HOLD_THROTTLE_SCALE = 0.5   # 降级期间的油门比例

# ---------------------------------------------------------------- 油门
CRUISE_THROTTLE = 100.0     # 常态巡线油门（百分比）
CONE_THROTTLE_SCALE = 0.5   # 见到锥桶时的油门比例
ZEBRA_THROTTLE_SCALE = 0.6  # 接近斑马线时的油门比例（P1-2 接入减速曲线前先用比例）
