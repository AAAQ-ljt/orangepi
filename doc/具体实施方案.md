# 2026 智能车 — 具体实施方案 v1.1（定稿）

> 状态：车已到货、SSH 隧道已通、frp 已部署、仓库已统一（AAAQ-ljt/orangepi）
> 依据：《2026重构设计文档.md》（架构）、《车到前预研清单.md》（清单）、2026 规则初稿（8 月版）
> 分工环境：**A 机 DSH** → 写小车 Python 代码；**本机 DSH** → 训练 YOLO；两台机共同提交 `AAAQ-ljt/orangepi` master
> 比赛时间：11 月中旬（约 10 周筹备期）

---

## 0. 定稿决策（2026-09-02 全员拍板，后续执行以此为准）

| # | 决策项 | 结论 |
|---|---|---|
| D1 | 巡线方案 | **OpenCV 扫线先行**（官方镜像自带，求中线稳快），YOLO 车道分割作为后续升级（骨架提前兼容两种） |
| D2 | 车端代码组织 | **全新目录 `/root/2026-orange-pi`**，我方代码全部放新文件夹，**不碰官方任何演示代码**（官方 frpc/nginx/网页控制完整保留可回退） |
| D3 | 红绿灯策略 | **识别灯色状态**：红灯停、绿灯行（状态机轮询灯色，不预测倒计时） |
| D4 | 供电 | **不更改供电**：小车做了特殊处理、螺丝无法拧开，无法加装外部电源 → 设计文档 §2.1 外接电源方案**挂起**；以软件降载 + 温压监测兜底（见 §9） |
| D5 | 数据集 | 规则道具**已购买**；流程 = 按规则搭小场景实拍 → 迭代训练 → 操场实拍补数据（从小场景到操场循序渐进） |
| D6 | 排期 | 比赛 11 月中旬，时间充裕；不设硬性冲刺盒，**以里程碑质量 + 兜底机制打磨 + 模型迭代**为主线 |

---

## 0.1 现状盘点（已完成 ✅ / 进行中 🔄）

| 项 | 状态 |
|---|---|
| 手机热点局域网 + SSH 直连（10.68.1.43） | ✅ |
| frp 服务器（121.40.149.155）+ SSH 2222 + 网页 8080 | ✅ 公网全通 |
| GitHub 仓库统一（fork 已删/PR 已合并） | ✅ |
| 小车 2026 规则初稿要点提取 | ✅（红绿灯为 2026 新元素） |
| 图传通道决策（官方 car0003 为主，8080 冗余，自建兜底） | ✅ 已定案 |
| 小车硬件环境探测（Python/RKNN/摄像头/IO） | ⏳ 下一步 |
| 2026 骨架代码 | ⏳ |

---

## 1. 网络与协作架构（定稿）

```
A 机(DSH 写车端代码) ──git──▶ AAAQ-ljt/orangepi ◀──git── 本机(DSH 训 YOLO)
        │                                                │
        ▼                                                ▼
   ssh -p 2222 root@121.40.149.155                 conda yolo11 (RTX 5070)
        │ frp 隧道                                    训练/ONNX/RKNN 转换
        ▼
   小车 OrangePi5（Ubuntu 20.04, Python 3.8）
```

**约定**：所有代码/配置只进 `AAAQ-ljt/orangepi` master；改动前备份；`scripts/ssh_car.py`（隧道）为唯一远程操作入口（`--direct` 兜底）。

## 2. 车端环境探明清单（2 天内，A 机 DSH 执行，只读优先）

- [ ] `cat /proc/cpuinfo | grep RK3588` 、`python3 --version`（已知 3.8.10）
- [ ] 摄像头枚举：`ls /dev/video*` + `v4l2-ctl --list-devices`（USB 还是 CSI）
- [ ] 帧率测试：官方 `/root/opi-control` 或 `ffmpeg -f v4l2` 抓 30 帧测延迟
- [ ] `i2cdetect -y 5` → PCA9685 地址（设计文档预期 0x40）
- [ ] IMU/GPS 设备节点（/dev/ttyS*）、CH340 串口
- [ ] ~~供电实测（USB-C 功率计）~~ → **D4：供电不更改**（螺丝不可拆）；改为：满载运行监测温度/重启记录（`journalctl --since=today | grep -i shutdown` + 温度日志），评估是否需要软件降载策略
- [ ] 5G 模块状态（SIM8202E-M2）、frpc 官方通道 car0003 可用性
- [ ] **实测官方通道图传延迟 vs 我们 8080 通道**（同一画面打点对比）→ 图传决策输入

## 3. 2026 任务链（按规则初稿，覆盖设计文档 §4.3 更新）

```
遥控阶段(5G)  1 起点 ──遥控──▶ 2 切换区（停入、启动自动驾驶）
自主阶段      挡板移除(蓝挡板=发车信号)
             → TRACKING 沿跑道线巡线
             → ZEBRA_STOP 斑马线 停10s + 播报"我是***（学校）***(队名)的智能车，请为我加油！"
             → TRAFFIC_LIGHT 红绿灯:光电触发 3~10s 随机倒计时,红灯停、绿灯通行 ★2026新
             → AVOID 两处锥桶(蓝/红锥随机位置,靠左/中/右) ★随机
             → PARKING 上下两个停车区,裁判随机放挡板 → 停未被挡的区(四轮入区)
             → 30s 内远程扫码付 0.01 元(+5 分)
```

> **对感知/控制的影响（重要）**：
> - 红绿灯倒计时**随机 3~10s** → 必须**识别红灯/绿灯状态**（smartcar.yaml 已有 traffic_light_red/green），不能死计时
> - 锥桶位置随机 → 近端避障必须有实时性（感知→控制 <80ms 目标）
> - 停车区上下随机 → 识别蓝色挡板位置，选未遮挡区 → FSM 增加 TRAFFIC_LIGHT 状态，删除 2025 的变道/引导区状态（规则无此元素）
> - 人行道停车 10s + 播报（2026 初稿，非 5s）

## 4. 骨架代码落地（A 机 DSH，按设计文档 §5 目录）

```
2026-orange-pi/          ← 建议以独立包置于车 /root/2026-orange-pi（不动官方任何东西）
├── main.py              # 装配/自检/信号处理(优雅退出→SafetyStop)
├── config/              # YAML + pydantic(3.8 兼容)
├── perception/          # camera / rknn / postprocess / lane_fit / filter
├── control/             # fsm(表驱动解释器+状态) / pid / speed_planner
├── hardware/            # pca9685/servo/esc/gimbal/imu/gps/audio/camera + mocks/
├── teleop/              # 手动驾驶:复用官方网页控制(car0003/8080)+ 自研键盘/手柄服务(进阶)
├── recorder/ replay/    # 录制/回放闭环(每轮联调必录)
├── telemetry/           # FastAPI 仪表盘 + JSONL 日志
├── tools/               # 采集/伪标注/rknn 转换
└── tests/               # pytest + 录播回归
```

**里程碑 M1（骨架，3 天）**：目录 + HAL 接口(Protocol)+ Mock + PerceptionFrame/ControlCommand + FSM 解释器跑通 BOOT→DONE（假数据演练）→ 全量单测过
**里程碑 M2（硬件点亮，2 天）**：PCA9685 驱动 + 舵机/电调标定 + 摄像头取流 + 安全自检（电调解锁时序、SafetyStop 信号路径）
**里程碑 M3（感知上板，取决于数据）**：RKNN 模型 + FPS/延迟基准 ≥25FPS / <80ms
**里程碑 M4（控制联调）**：FSM 全状态真车跑，录播复盘调参
**里程碑 M5（手动驾驶）**：图传 + 摇杆 + 5G 遥控跑通（先官方通道）
**里程碑 M6（模拟赛+手册）**：全流程 3 次模拟跑 + 5 案例技术手册

## 5. 数据集与模型（本机 DSH，RTX 5070）

| 类别 | 数据来源 | 目标量 |
|---|---|---|
| traffic_cone(红/蓝) | 实拍采集（tools 采集器）+ 增强 | ≥300 |
| blue_barrier | 实拍（发车/停车场景） | ≥200 |
| traffic_light_red/green | 实拍倒计时场景（重点!） | ≥300/300 |
| crosswalk | 实拍 | ≥200 |

流程：新车摄像头实拍 → 2025 权重(best11sseg)伪标注 → 人工修正(labelimg) → 亮度/阴影增强 → 微调（迁移学习）→ 每类 mAP 验收 → `.pt→ONNX→RKNN(FP16)` 在 PC 模拟器验证 → 上板基准。

## 6. 图传决策（已定案：官方通道为主，自建兜底）

**结论（2026-09-02 定案）**：car0003 = 本车官方出厂子域名（每车一个，如 004/005/006）；图传链路 = 小车画面 → WebRTC → 官方媒体服务器(82.157.204.126:17001) → 浏览器（页面入口无论官方子域还是我们 8080，whip/whep 均指向官方，出厂写死）。
- **主线**：图传直接走**官方通道**（官方维护、零自建成本）
- **我们的 8080 通道**：页面/摇杆/控制的冗余备份（视频仍过官方）
- **兜底**：若比赛当天官方服务器拥塞/不可用 → 启动自建 mediamtx（WebRTC SFU）+ frp 加映射 + 改 nginx whip/whep 指向（已记 ADR 待办）
- **比赛纪律**：遥控/图传阶段必须使用小车自带 4G/5G 模块（规则罚则：使用局域网/写死 +200s）
- 残留待办：2026 正式规则是否恢复"回传裁判屏幕"条款（QQ 群 136310012 可先问）；近期加 frpc-ssh.ini `pool_count=5` 消隧道尖峰（1 行，待批准执行）

## 7. 仓库与协作铁律

- 唯一远端：`origin = AAAQ-ljt/orangepi`；**每天收工前 push**（网络断了由后台重试任务兜底）
- 白名单：code/ + doc/ + scripts/（.gitattributes 已固定 LF）
- 车端改动先备份再改；systemd/驱动级改动须经队长确认

## 8. 里程碑排期（比赛 11 月中旬，约 10 周；按质量推进，不设硬冲刺）

| 阶段 | 内容 | 出口标准 |
|---|---|---|
| P1 环境探明 | 摄像头/I2C/串口/温度监测/RKNN 工具链确认 | 清单全项有结论 |
| P2 骨架 M1 | 目录+HAL(Mock)+数据契约+FSM 解释器+录播框架 | BOOT→DONE 假数据演练 + 单测全绿 |
| P3 硬件点亮 M2 | PCA9685+舵机/电调标定+摄像头取流+SafetyStop | 上电自检一次通过、遥控可动 |
| P4 感知 M3 | 小场景数据→微调→RKNN 上板 | ≥25FPS / <80ms 基准达标 |
| P5 控制 M4 | FSM 全状态真车联调（录播复盘循环） | 单任务（斑马线/红绿灯/锥桶/停车）各自稳定 3 连过 |
| P6 手动驾驶 M5 | 官方通道图传+摇杆+5G 遥控 | 全流程遥控跑通、延迟记录在案 |
| P7 兜底演练 | 掉电/断网/撞桶/识别丢失等故障注入演练 | 每项有应对动作且验证过 |
| P8 模拟赛+手册 | 全流程模拟跑 ≥3 次 + 5 案例技术手册 | 完赛 + 手册提交 |

> 说明：D6 定案时间充裕，P4~P5 之间按数据质量迭代（小场景→操场），不赶工；兜底演练（P7）为 2026 重点新增。

## 9. 风险备忘

| 风险 | 应对 |
|---|---|
| 红绿灯倒计时随机（3~10s）识别不稳 | 数据重点采倒计时场景；FSM 红/绿双态 + 连续帧滤波 |
| 锥桶随机位置撞桶(+20s/拖桶2m=失败) | 近端避障优先保"不撞"，宁可慢 |
| 手机热点不稳（遥控阶段裁判可能查"局域网"） | 比赛用手车 4G/5G 模块，非热点；日常开发热点可接受 |
| frp 隧道延迟波动 | pool_count 预连接（待批准）+ 图传以官方通道为主，官方拥塞为共同风险 |
| **供电（D4：不可改造）** | 螺丝不可拆、不装外部电源 → 软件降载：关闭无关服务、推理帧率分级（高负载降帧）、监测温度/意外重启日志；若出现掉电重启，记录触发条件并对应降载 |
| 官方图传服务器比赛当天拥塞 | 8080 通道页面/摇杆冗余 + 自建 mediamtx 兜底（已列 ADR 待办） |