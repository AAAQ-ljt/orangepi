# 图传与工作模式方案（自建服务器版）

> 建立于 2026-09-15。取代原"官方媒体服务器为主"的方案（`具体实施方案.md` §6 已同步更新）。
> 涉及：**摄像头推流 → 自建服务器 121.40.149.155 → 浏览器**，以及**手动遥控 / 自动驾驶**两种模式的切换。

---

## 0. 结论速览

| 项 | 结论 |
|---|---|
| 媒体服务器 | **自建 mediamtx v1.21**，装在 `121.40.149.155:/opt/mediamtx`，systemd 服务 `mediamtx.service` |
| 推流方向 | **小车主动推** RTSP 到 `rtsp://car:<密码>@121.40.149.155:8554/cam_car0027`（`_sub` 为副摄） |
| 播放 | 网页用 **WHEP（WebRTC）**，延迟最低；HLS 作为兜底 |
| 官方服务器 | **不再使用**（`82.157.204.126` 的推流与 whip/whep 反代都已改掉） |
| 网页地址 | `http://121.40.149.155:8080/`（走我们的 frp 隧道，和以前一样） |
| 模式切换 | `bash /root/dev/scripts/car-mode.sh <status\|auto\|manual\|estop\|stream\|logs>` |
| 上电默认 | **手动遥控**（opi-control + 网页 + 推流），自动驾驶必须手动敲命令进入 |

---

## 1. 链路（切换后）

```text
小车                                自建服务器 121.40.149.155                浏览器
────                                ────────────────────────                ──────
主摄 video0（固定）─┐
                    ├─ ffmpeg ──RTSP──▶ mediamtx :8554  (cam_car0027 / _sub)
副摄 video2（云台）─┘                       │
                                            ├─ WebRTC :8889(WHEP 信令) + :8189(ICE)
                                            └─ HLS    :8888 (兜底)

入口（两条，同一个服务）：
  http://121.40.149.155:8080/        （IP + 端口）
  http://liyekai380.dpdns.org/       （域名，经 frp remote_port 80，需安全组放行 80）

小车 nginx :80
  ├─ /                 网页（摇杆 + 播放器）
  ├─ /ws               opi-control 遥控指令通道
  ├─ /cam_*/whep       反代到 服务器:8889 → WebRTC 拉流（低延迟）
  └─ /hls/...          反代到 服务器:8888 → HLS 兜底

语音下行：talk-player 从 mediamtx:8554/talk_car0027 拉流播放（原官方 82.157.204.126 已停用）
官方通道：frpc.service 与官方推流**已停止并 disable**（原配置备份在车端 /root/dev/backup-20260915/）
```

**为什么改 nginx 就够、不用动网页？** 网页里的 WHEP 地址是
`` `${location.protocol}://${window.location.host}/${streamPath}/whep` `` —— **同源**地址，
所以只要改小车 nginx 的 `proxy_pass` 目标，前端一行都不用动。

**流名**：网页默认 `streamPath = cam_car0027`、副摄 `cam_car0027_sub`
（那段"按域名智能生成流名"的代码在 `return` 之后，是死代码，永远不会执行）。

---

## 2. 需要放行的端口（★ 只差这一步）

阿里云控制台 → 实例 → 安全组 → 入方向，放行：

| 端口 | 协议 | 用途 | 必须性 |
|---|---|---|---|
| `8554` | TCP | **小车推流入口**（RTSP） | ★必须，否则图传完全不通 |
| `8889` | TCP | WebRTC 信令（网页 WHEP） | ★必须，网页里的画面靠它 |
| `8189` | TCP | WebRTC 媒体（ICE over TCP） | ★必须（只开 TCP 也够用） |
| `8189` | UDP | WebRTC 媒体（ICE over UDP，延迟更低） | 建议 |
| `8888` | TCP | HLS 兜底播放页 | 建议（WebRTC 出问题时救场） |

已放行：`7000`（frp 控制）、`2222`（SSH）、`8080`（网页）。

> 放行后确认（在任意能上网的机器上）：
> ```bash
> python -c "import socket;s=socket.socket();s.settimeout(4);s.connect(('121.40.149.155',8554));print('8554 ok')"
> ```

---

## 3. 小车端改了什么（可回滚）

| 文件 | 改动 | 备份位置 |
|---|---|---|
| `/etc/systemd/system/ffmpeg-stream.service` | 推流地址 → 自建服务器；地址抽到 `/etc/default/smartcar-media` | `/root/dev/backup-20260915/` |
| `/etc/systemd/system/ffmpeg-stream-sub.service` | 同上（副摄） | 同上 |
| `/etc/nginx/sites-enabled/orangepi-rc` | `whip/whep` 反代目标 → 自建服务器；新增 `/hls/` 兜底 | 同上 |
| `/etc/default/smartcar-media` | **新增**：`MEDIA_RTSP=rtsp://car:<密码>@121.40.149.155:8554` | — |

仓库里对应留存：`dev/scripts/car-ffmpeg-stream.service`、`car-ffmpeg-stream-sub.service`、
`car-nginx-orangepi-rc.conf`、`car-media.env`。

**换服务器地址**只改 `/etc/default/smartcar-media` 一行，然后
`systemctl restart ffmpeg-stream ffmpeg-stream-sub`。

**回滚到官方服务器**（应急）：
```bash
cp /root/dev/backup-20260915/ffmpeg-stream.service /root/dev/backup-20260915/ffmpeg-stream-sub.service /etc/systemd/system/
cp /root/dev/backup-20260915/orangepi-rc /etc/nginx/sites-enabled/orangepi-rc
systemctl daemon-reload && systemctl restart ffmpeg-stream ffmpeg-stream-sub && nginx -s reload
```

---

## 4. 服务器端（已部署好）

| 项 | 值 |
|---|---|
| 程序 | `/opt/mediamtx/mediamtx`（v1.21.0，x86_64） |
| 配置 | `/opt/mediamtx/mediamtx.yml`（原始模板备份为 `mediamtx.yml.default`） |
| 服务 | `systemctl {status,restart} mediamtx`，开机自启 |
| 发布账号 | `car` / 密码见配置与 `dev/scripts/car-media.env`（**只允许发布 `cam_*`、`talk_*`**） |
| 读取 | 开放（裁判/司机打开网页即可看，不需要密码） |
| 管理 API | `curl -s http://127.0.0.1:9997/v3/paths/list`（仅本机） |
| 资源限制 | `MemoryMax=256M`、`CPUQuota=120%`（和 frps 共用 2 核 1.6G 的机器） |

查看流状态（在服务器上）：
```bash
curl -s http://127.0.0.1:9997/v3/paths/list | python3 -m json.tool | head -30
journalctl -u mediamtx -n 30 --no-pager
```

---

## 5. 工作模式切换（car-mode.sh）

上电默认**手动遥控**。要跑自主，在小车（或通过 SSH）敲：

```bash
bash /root/dev/scripts/car-mode.sh status              # 看当前模式/服务/进程/温度/摄像头占用
bash /root/dev/scripts/car-mode.sh auto                # 进入自动驾驶【默认 DRY-RUN，电机不动】
bash /root/dev/scripts/car-mode.sh auto --real         # 真正跑车（要输入 GO 确认）
bash /root/dev/scripts/car-mode.sh manual              # 回到手动遥控
bash /root/dev/scripts/car-mode.sh estop               # 紧急停止（立刻归零）
bash /root/dev/scripts/car-mode.sh stream up|down|status   # 图传推流控制
bash /root/dev/scripts/car-mode.sh logs vision|control|stream
```

`auto` 的选项：`--real`（使能动力）、`--camera N`（0=主摄/2=副摄）、`--model PATH`（RKNN 模型）、
`--max-us N`（电调上限，默认 1600；**1500 停、≈1545 才起转**）、`--port N`、`--minimal`（连网页/另一路推流也停，省 CPU）、`--yes`（跳过确认）。

**这个脚本替你保证了四件事**（旧脚本的坑都补上了）：

1. **进前体检**：python 依赖、摄像头存在且空闲、PCA9685 在线（非 dry-run 时）、UDP 端口空闲、媒体服务器可达性；
2. **只停必须停的服务**：抢 PCA9685 的 `opi-control`、占音频的 `talk-player`、**占用我们要用那路摄像头的推流**；
   **另一路推流保留**，裁判仍能看到画面（规则要求全程回传）；
3. **任何退出路径都恢复手动模式**：正常结束、Ctrl+C、SSH 断线（SIGHUP）都会触发同一个 `cleanup`，
   不会把车留在"既没遥控也没自驾"的中间态；若媒体服务器不可达，会跳过两个推流服务，避免 3 秒一次的重启循环；
4. **视觉没起来就不放行**：等视觉写出 `/tmp/smartcar_status.json`（说明真出帧了）才启动控制进程。

模式判定（`status`）：
- `control.pid` 活着 → **自动驾驶中**
- 否则 `opi-control` active → **手动遥控**

---

## 6. 摄像头角色（2026-09-16 图片确认）

| 设备 | 型号 | 角色 | 用途 |
|---|---|---|---|
| `/dev/video0`（index 0） | icspring（USB） | **主摄 ＝ 云台摄像头** | 装在两轴云台上；红绿灯环节抬头看灯；推流 `cam_car0027`（**裁判/司机看的主画面**） |
| `/dev/video2`（index 2） | Global Shutter（USB） | **副摄 ＝ 下摄** | 朝向赛道前方地面；**巡线扫线就用它**；推流 `cam_car0027_sub` |

推论与待办：

- **自动驾驶默认 `--camera 2`**（下摄），`car-mode.sh auto` 默认值即如此；此时停 `ffmpeg-stream-sub`，
  **保留 `ffmpeg-stream`（云台主摄）给裁判看**——云台往前看，恰好是最适合判罚的视角；
- 红绿灯环节需要抬头看灯 → 切到云台摄（video0）（P1-3 一并做），届时会与主推流冲突，按下面的"画面连续性"处理；
- ⚠️ **切换相机时的画面连续性**：自动驾驶时我们占用哪一路，那一路的推流就必须停。
  **解决方向（P1-3 一并做）**：让 `vision_main.py` 把**当前正在处理的那一帧**（无论是哪一路）直接推给媒体服务器，
  这样裁判看到的永远是我们视觉所用的画面——既解决冲突，也最有判罚说服力。
- 想复验映射：`sudo python3 /root/dev/scripts/cam-identify.py`（移动云台，画面跟着动的那路就是云台摄）。

## 6.1 比赛日的用法（合规）

规则要求比赛过程中把实时画面回传裁判：

- 默认（巡线阶段）：视觉用下摄（video2）→ 停 `ffmpeg-stream-sub.service`，
  **保留 `ffmpeg-stream.service`（云台主摄）** 给裁判；`car-mode.sh auto` 不加 `--minimal` 时就是这个行为；
- 红绿灯阶段：见上面的"待办"，在 P1-3 落地"视觉自己推流"之前，先用主摄画面顶一下；
- 比赛纪律：遥控/图传阶段必须走小车自带 4G/5G 模块（用局域网/写死 +200s）。

---

## 7. 排障

| 现象 | 原因 / 处理 |
|---|---|
| 网页能开，但画面黑屏 | ①安全组没开 `8889`/`8189`；②小车推流没起：`car-mode.sh stream status`；③服务器 `curl 127.0.0.1:9997/v3/paths/list` 看流在不在 |
| 推流服务反复重启 | 服务器 `8554` 不通（安全组/网络）。`car-mode.sh stream up` 会先提示；日志 `car-mode.sh logs stream` |
| 网页 WHEP 报 4xx/5xx | 小车 nginx 反代目标写错：`grep -n whep /etc/nginx/sites-enabled/orangepi-rc` 应为 `121.40.149.155:8889` |
| 只有一路画面 | 网页播放器要切主/副摄；两路流名分别是 `cam_car0027` / `cam_car0027_sub` |
| 延迟大（>2s） | 说明在用 HLS 兜底；检查 `8889`+`8189` 是否放行（WebRTC 未生效时会退化） |
| ssh 上传远端路径报 `No such file` | **Git Bash 路径转换坑**：命令前加 `MSYS_NO_PATHCONV=1`（见 AGENTS.md §6） |

---

## 8. 网络故障恢复（2026-09-16 实战复盘）

**现象**：小车重启后 WiFi 与流量卡都连不上、frp 隧道掉线、SSH 全部失联。

**诊断结论（三条）**：

1. **蜂窝其实一直是好的**——由 `/etc/rc.local` 调用的**官方 `simcom-cm`**
   （`/root/SIM8200_for_RPI/Goonline/`）负责拨号。
   团队后加的 `car-dial.service`（跑 `/root/car_net_up.sh`，用 qmicli 手拨）
   **从来没成功过**（日志里 `OK IP=` 计数为 0），而且它每次都会 `ip addr flush dev wwan0`
   并抢 QMI 设备，**把官方拨号已建立的连接打断**——表现就是"流量卡连不上"。
   → **处置：`systemctl disable --now car-dial.service`**（脚本保留；
   真需要手动拨号时先确认 simcom-cm 没在跑，再 `bash /root/car_net_up.sh`）。
2. **WiFi**：`Aaaq` 热点重启后卡在 activating / connecting(getting IP configuration)；
   `nmcli con down Aaaq && nmcli con up Aaaq` 后恢复正常（关联成功、拿到地址）。
3. **网络全断时唯一入口 = 串口**：CH340 → `COM8` / **1500000** / 8N1。
   用 `python scripts/serial_car.py "命令"` 免交互操作（自动登录 + 标记捕获），
   详见 AGENTS.md §4.2。

**恢复顺序（照这个顺序做，最快）**：
```text
串口能进 → 看 wwan0 有没有 IP（ip -4 a show wwan0）
        → 有 IP 但连不通：查默认路由（ip r）与 dns（resolvectl status）
        → 没有 IP：确认 simcom-cm/rc.local 是否执行；必要时手动拨号
        → 网络通后看隧道：systemctl status frpc-ssh（应有 start proxy success）
        → 页面：http://121.40.149.155:8080/
```

**安全组现状（2026-09-16）**：已放行 `7000` `8080` `8554` `8889` `8189(TCP/UDP)` `8888`；
**待补**：`80`（域名 `liyekai380.dpdns.org` 裸域名入口）、`22`（服务器自身 SSH）、`2222`（小车 SSH 隧道）。
