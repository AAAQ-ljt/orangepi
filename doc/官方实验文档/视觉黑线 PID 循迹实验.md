# **视觉黑线 PID 循迹实验**

## **实验目的**

1、掌握 OpenCV C++ 完整图像预处理流程：灰度化、高斯模糊、自适应二值化、ROI 区域裁剪。

2、理解自定义像素遍历函数 find_RoadCenter 原理：逐像素统计白色轨迹点，自主计算道路中心坐标，不依赖轮廓 / 图像矩函数。

3、掌握横向偏移误差计算逻辑：道路中心与 ROI 画面中心做差得到 offset 偏差值。

4、掌握增量式 / 位置式 PID 控制器参数配置、实时更新逻辑，实现舵机连续角度纠偏。

5、熟悉 PCA9685 I2C 驱动芯片控制电机、舵机的底层接口调用，实现小车前进、转向、停机控制。

6、掌握 Linux 信号捕获、互斥锁线程同步、UDP 上位机调参、摄像头资源安全释放等嵌入式 C++ 工程规范。

7、理解循迹可视化原理，能根据二值图轨迹中心点分析小车偏移状态，完成 PID 参数调优。

## **实验内容**

1、USB 摄像头采集 640×480 分辨率画面，裁剪指定 ROI 区域过滤无效背景，降低图像运算量。

2、对 ROI 图像依次执行灰度转换、高斯模糊降噪、二值化处理，将黑色轨迹转换为白色前景像素。

3、遍历 ROI 所有像素，统计白色轨迹像素总数，加权平均计算道路中心点 PointInfo center。

4、对比道路中心横坐标与 ROI 理论中心 IMG_CENTER，计算偏移误差 offset。

5、PID 控制器实时读取误差，输出调节量映射至 PCA9685 舵机 PWM 数值，控制前轮转向修正轨迹；电机固定输出前进 PWM。

6、实现阈值保护逻辑：白色像素数量过少时判定无轨迹，电机停机、舵机回中。

7、可选开启 UDP 多线程上位机通信，实时在线修改 PID 参数、观测误差波形；支持窗口可视化调试。

8、程序捕获退出信号，自动停机，释放摄像头、PCA9685、线程、互斥锁全部硬件内存资源。

## **实验仪器**

1、OrangePi5。

2、USB 摄摄像头（分辨率 640×480）。

3、PCA9685 I2C 多路 PWM 驱动模块、直流减速电机、转向舵机、配套电源。

4、白底黑色胶带循迹赛道。

5、电脑 MobaXterm 远程连接工具（开启 X11 图像转发）。

6、预装 OpenCV4、I2C 库、pthread 线程库的 C++ 编译环境。

## **实验原理**

1、图像预处理原理

（1）灰度化：cvtColor(frame, gray, COLOR_BGR2GRAY)，将三通道彩色图转为单通道灰度图，仅保留亮度信息，减少计算开销。

（2）高斯模糊：GaussianBlur(gray, blur, Size(GZ,GZ), 0)，平滑图像，消除地面灰尘、光斑产生的噪点像素，避免干扰中心点计算。

（3）二值化：通过宏 BINARY_MODE 切换正向 / 反向二值；本实验黑线赛道使用 THRESH_BINARY_INV，黑色轨迹灰度低，转换后像素值 255（白色），地面背景为 0（黑色）。

（4）ROI 裁剪：通过宏 ROI_X1/ROI_Y1/ROI_X2/ROI_Y2 划定画面下方有效区域，过滤上方无关背景，缩小像素遍历范围。

2、道路中心像素遍历计算逻辑

自定义函数 find_RoadCenter(Mat img, int &white_point_count) 双层循环遍历 ROI 内每一个像素：仅累加像素值 = 255 的白色轨迹坐标，统计总白点数量。

道路中心横坐标：center.x = ( Σ 所有白点 x 坐标 ) / white_point_count

道路中心纵坐标：center.y = ( Σ 所有白点 y 坐标 ) / white_point_count

若白点总数小于 WHITE_MAXNUM_STOP，判定赛道丢失，小车立即停机。

3、循迹二值图中心点示意图

**图 1  循迹二值图中心点示意图**

 

示意图说明：红色框为 ROI 裁剪区域，黄色圆点是道路轨迹中心 x₁，绿色圆点是 ROI 理论车头中心 x₂。

offset = x₁ − x₂

offset > 0：轨迹中心偏右，舵机向右修正；

offset < 0：轨迹中心偏左，舵机向左修正；

offset ≈ 0：轨迹居中，舵机保持中位直行。

4、PID 闭环控制原理

程序内置独立 PID 结构体，包含 Kp、Ki、Kd、输出限幅、积分上下限参数：

（1）初始化阶段：PID_Init() 载入宏定义 KP/KI/KD，限制输出区间 OUT_MIN~OUT_MAX、积分饱和区间；

（2）循环更新：将 IMG_CENTER 设为目标值，道路中心 center.x 作为实际值，传入 PID_Update 计算调节输出；

（3）输出映射：PID 输出值叠加舵机中位 PWM，限制在 SERVO_MIN~SERVO_MAX 区间，通过 PCA9685 修改舵机通道占空比，实现连续转向纠偏；

（4）死区 DEAD_ZONE：微小偏移不输出转向，避免小车直线来回抖动。

5、PCA9685 驱动硬件控制

I2C 通信控制 PCA9685 芯片两路通道：

CH_SERVO (0)：前轮转向舵机，中位 1500，左极限 2000，右极限 1000；

CH_MOTOR (1)：驱动电机，前进固定 1646，停机 1600；封装安全写入函数 pca9685_write_us_safe，防止硬件 PWM 突变冲击。

6、多线程与信号处理

（1）信号捕获：注册 SIGINT 终止信号回调函数，按下 Ctrl+C 时执行停机流程；

（2）互斥锁 pthread_mutex_t：保护 PID 参数、图像数据多线程读写冲突；

（3）UDP 子线程：(可选)开启上位机通信，实时在线修改 Kp/Ki/Kd，观测误差曲线，无需重启程序。

## **实验步骤**

**一****、查看程序宏定义与源码结构**

cd /root/car_Tracking，打开源码 line_follow.cpp，代码顶部包含头文件引用、数据结构声明及调试配置宏。以下为关键配置代码段：

**可以修改的参数有:****ROI_X\*/ROI_Y*用于设置ROI区域;****GZ用于设置高斯滤波值;****SHOW_IMAGE设置显示实际画面;**
**WHITE_MAXNUM_STOP****设置停车阈值;****BINARY_MODE设置二值化模式;****USE_UDP设置是否使用上位机调参;**

**SPEED_RUN,SPEED_STOP电机值;**

**KP,KI,KD** PID参数值

核心函数声明如下：

 

**三、main 主函数初始化流程**

main 函数依次完成信号注册、PCA9685 初始化、PID 参数配置、互斥锁初始化、强制停车回中、摄像头初始化。

  **main 函数初始化段**

 

**四、图像处理与循迹逻辑**

主循环中摄像头抓取最新帧，依次执行灰度化、高斯模糊、二值化处理，再调用 find_RoadCenter 计算道路中心点。

  **摄像头抓取与主循环结构**

 

  **图像预处理（灰度化、高斯滤波、二值化）**

 

**五、PID 闭环控制与舵机执行**

以 IMG_CENTER 为目标值、道路中心 center.x 为实际值计算 PID 输出，经死区判定后映射为舵机 PWM 数值写入 PCA9685。

  **PID 计算、死区判定与舵机 PWM 输出**

 

**六、退出流程与资源释放**

程序退出前执行强制停车循环，依次释放摄像头、PCA9685、窗口、互斥锁等硬件资源，并重启相关服务。

  **退出停机与资源释放代码**

 

**七、上位机辅助调参程序（可选）**

**香橙派端需要先把car_Tracking目录下的config.txt中两个ip改为对应的ip值，PC_HOST填主机地址，ORANGEPI_IP填香橙派地址**

![img](file:///C:\Users\luo20\AppData\Local\Temp\ksohtml64068\wps11.jpg)

本工程配套一个 Python 上位机程序，通过 UDP 接收小车回传的道路中心位置并实时绘制曲线，同时提供 Tkinter 参数面板可一键下发 KP/KI/KD 和电机脉宽，无需重新编译即可在线调参。运行命令如下：

pip install matplotlib tkinter

python pid_tuner.py

上位机完整代码如下：

 

运行后界面左侧为实时位置曲线（蓝线为实际位置、红线为目标位置 320），右侧为参数输入面板，修改参数后点击一键下发全部PID参数即可在线调整。需在 line_follow.cpp 中将 USE_UDP 宏设为 1 才能启用上下位机通信。

 

## **实验结果**

1、成功完成 USB 摄像头 640×480 画面采集与 ROI 区域裁剪，有效过滤无关背景。

2、成功实现灰度化、高斯模糊、二值化的图像预处理流水线，黑色轨迹被正确转换为白色前景像素。

3、成功通过 find_RoadCenter 函数逐像素统计道路中心点，与理论中心对比得到偏移误差 offset。

4、成功运行 PID 闭环控制器，小车在直道与弯道上均能自动修正方向，沿白色/黑色轨迹稳定循迹。

5、触发停机条件（如白色像素点过少、摄像头无帧）时，电机及时停机、舵机回中，实现安全保护。

6、程序退出时正确释放所有硬件资源，无内存泄漏和 I2C 总线占用异常。

## **结果分析**

1、ROI 裁剪将有效计算区域限制在画面下方 205–405 行，显著减少像素遍历数量，提高实时性。

2、高斯模糊核大小 GZ=7 在降噪与边缘保留之间取得平衡，过大会模糊轨迹边缘，过小则噪点过多影响中心计算。

3、二值化阈值 THRESHOLD=195 是实验关键参数：过低会把地面阴影误判为轨迹，过高会漏掉浅色轨迹，需根据实际赛道光照调整。

4、PID 参数 KP=7.0、KD=3.0 为经验调优值：Kp 太小响应慢、太大则抖动；Kd 有效抑制过冲，但过大会对噪点敏感,Ki不建议使用。

5、WHITE_MAXNUM_STOP=30 作为丢线判定阈值，实际使用中应根据赛道宽度和摄像头高度调整，过低会在弯道误判，过高会延迟响应。

 