# 实地调试清单（操场）

> 建立于 2026-09-19。用途：**到了操场照着做**，一项一项过，每项只记一条数据；不通过就不往下走。
> 相关文档：`执行路线图.md`（做什么/为什么）、`自动驾驶开发方案.md`（块 A/B/C 的技术方案）。
> ⚠️ 实验室的打印跑道是**亮面覆膜**，反光严重（白线只比赛道亮约 25 级、反光比线亮 40 级），
> 实验室里的扫线读数**不能代表操场**。一切以操场实测为准。

---

## 0. 出发前（实验室，10 分钟）

```bash
MSYS_NO_PATHCONV=1 python scripts/ssh_sync.py      # 代码同步到车
python scripts/ssh_car.py "bash /root/dev/scripts/run_tests.sh"   # 车端单测（应 13/13）
python scripts/ssh_car.py "bash /root/dev/scripts/net/car-net.sh status"   # 网络（蜂窝或 WiFi 通一条）
python scripts/ssh_car.py "ls -la /root/dev/models/"              # 模型在车上
```

- [ ] 车端单测全绿、代码已同步
- [ ] 网络通（蜂窝优先、WiFi 账本兜底）、浏览器能看两路图传
- [ ] 模型：`models/best_4cls_640x480_fp16.rknn`（4 类，输入 480×640 原生）
- [ ] 电池充满 + 备用电池；急停/断电手段明确（谁负责断电）
- [ ] 带：卷尺、白纸/胶带、蓝板、锥桶、笔记本（记录用）

---

## 1. 到场地先做的三件事（**不许动车**）

```bash
# ① 扫线视角 + 光照：看白线在这片场地上到底什么样
sudo python3 /root/dev/scripts/diag/lane_probe.py --camera 2 --frames 4

# ② 模型自检：每个类 id 在这片场地上检出什么、框对不对
sudo python3 /root/dev/scripts/diag/model_probe.py \
     --model models/best_4cls_640x480_fp16.rknn --camera 2 --frames 5

# ③ 环境体检：模式/服务/温度/摄像头占用
bash /root/dev/scripts/car-mode.sh status
```

**记录表（第一次必须填）**

| 项目 | 读数 | 备注 |
|---|---|---|
| 扫线：白像素占比（过滤后） | ___% | 实验室是 3.5~10% |
| 扫线：最好置信度 | ___ | ≥0.4 才算可用 |
| 扫线：判定文本 | ___ | ✅/⚠️/❌ |
| 模型：id1（斑马线）分数 | ___ | 有斑马线时应明显 >0.25 |
| 模型：id3（蓝板）分数 | ___ | 板在车前时应 >0.5 |
| 模型：id0 / id2 | ___ | 红绿灯 / 锥桶，出现时确认 id 是否对 |

> 如果这里 `置信度 ≥ 0.4` → **扫线可以用**，块 A 的巡线就能开；否则走"不依赖白线"的路线（见 `自动驾驶开发方案.md` §2.6）。

---

## 2. 电调/机械项（**四轮架空**，或场地空旷 + 有人在旁）

按顺序，每项一条记录：

| # | 项目 | 命令 | 通过标准 | 记录 |
|---|---|---|---|---|
| 2.1 | 转向方向 | `bench_test.py --steer-test --allow-motion` | 前轮"角度大"是右转（否则写 `steer_sign: -1`） | ___ |
| 2.2 | 直行基准 | `bench_test.py --no-lane --allow-motion --max-seconds 8` | 直行不跑偏；偏了改 `site.yaml` 的 `servo_center_angle`（左偏调大 / 右偏调小，每次 1~2°） | 偏 ___ 侧 ___ cm / 8s |
| 2.3 | 蓝板发车 | `bench_test.py --no-lane --allow-motion` | 板在=停（1500us）；移开=走（1575us）；放回=立即停 | ___ |
| 2.4 | 电调速度标定 | `bench_test.py --no-lane --speed-us 1590 --allow-motion --max-seconds 5` | 记录"脉宽 → 大概速度"，为减速停准留数据 | 1590us ≈ ___ m/s |

> 电调死区：1500 停、**≈1545 才起转**；低于死区脚本会直接拒绝运行。

---

## 2.5 循迹扫线实跑（蓝板发车 → 循迹 → 再见蓝板停车）

**摄像头固定用下摄 `--camera 2`（云台不参与巡线）**。⚠️ **循迹档已统一到 §2.6 的
`lane_ref_test.py`**：`bench_test.py` 的扫线档随两条旧扫线模块一起归档了，现在它只保留
"转向方向 / 直行基准 / 蓝板发车 / 速度标定"这些**不依赖扫线**的项（见 §2 表格）。

板在车前 → 停（1500us）；移开 → 循迹；再放回正前方 → 停。
**板要摆在下摄的正前方**——发车与停车用的都是下摄这一路画面。

```bash
# ① 先不动电机：确认板能被认到、线能扫到、转向方向对不对
sudo python3 /root/dev/scripts/lane_ref_test.py --no-motor

# ② 正式跑（详细算法与调参见 §2.6）
sudo python3 /root/dev/scripts/lane_ref_test.py --allow-motion --max-seconds 120 \
     --log-csv /root/dev/logs/lane_ref.csv
```

**判据与调参对照表**（跑完看 `--log-csv` 或终端状态行；参数都可用命令行改，不用动代码）：

| 现象 | 看哪个数 | 怎么调 |
|---|---|---|
| 车不动 | 终端的阶段一直是 `(stop)`，或结尾出现"⚠️ 全程没见过蓝板" | 板没被认到：换更大的板 / 摆到下摄正前方更近处；先用 `lane_probe` 确认画面里蓝色占比 |
| 移开板后拱一下就停 | 阶段 `(explore)` 3s 后变回 `(stop)` | 探路窗口里没找到线 → 先按 §1 解决扫线；确需更久可 `--acquire-s 5` |
| 过弯切内道 / 压线 | 误差大时舵机角度不够 | `--kp 0.2`（默认 0.15）、必要时 `--limit-deg 20` |
| 蛇形 / 画龙 | 误差在正负之间来回过冲 | `--kd` 加大（0.12 → 0.3）、`--kp` 调小 |
| 越修越偏（冲出赛道） | 误差单调变大 | **转向方向反了** → `config/site.yaml` 写 `steer_sign: -1`；先用 §2.1 的 `--steer-test` 确认 |
| 速度不合适 | 状态行的 `电调=xxxx us` | `--speed-us 1590`（死区 1545；调试上限 1600） |

> ⚠️ 跑之前确认：场地空旷、有人在旁能立刻断电；`--max-seconds` 是硬上限；
> 任何退出方式（Ctrl-C / 超时 / 出错）都会**电调归零 + 恢复远程控制阶段**。
> 脚本运行期间**两路推流都会被停掉**（摄像头独占），浏览器看不到画面，靠终端状态行 + CSV 复盘。

## 2.6 参考实现版循迹（`lane_ref_test.py`，推荐先跑这个）

严格按参考实现（`oldCode/src/vision/vision.cpp` + `control.cpp`）写的独立脚本，
不掺我们自己的启发式；仓库里另外两套扫线（`lane_scan.py` / `lane_hough.py`）已归档到 `dev/attic/`。

> §1 的 `lane_probe` 现在也用同一个检测器（并按 `config/site.yaml` 的 ROI 标定），
> 两个工具的读数应当一致——不一致说明参数（`--roi-top` / `--band`）没对齐。

```bash
# ① 只看算法读数（不动电机；本地也能跑：加 --image <图片路径>）
sudo python3 /root/dev/scripts/lane_ref_test.py --no-motor

# ② 正式跑：板在→停；移开→循迹；再见板→停
sudo python3 /root/dev/scripts/lane_ref_test.py --allow-motion --max-seconds 120      --log-csv /root/dev/logs/lane_ref.csv
```

算法（照抄参考实现）：ROI → 灰度 → 高斯 → **Canny(60,140，按边缘数自适应并钳位）**
→ **HoughLinesP(1,3°,50,30,5)** → 斜率 |dy/dx| ∈ [0.25,2] → 按斜率符号分左右、
每侧取代表线 → 前瞻带逐行求中点平均 → error（像素）→ `angle = 90 - (kp·e + ki·∫e + kd·Δe)`
（kp/ki/kd = 0.15/0.01/0.12，限幅 ±15°，输出再平滑 0.7/0.3）→ 误差大时自动减速。

**两处按我们车改过的地方（都已实测标定，其余与参考一致）**：
- ROI 用 `0.35~0.58`（参考是 `0.50~0.85`）：我们下摄太"平"，那个区间里右线已经跑出画面；
- 目标点 `--target-ratio 0.586`（= 画面 375 处；参考是 226/320，那是它自己的安装）。
  另外选线加了"位置锚定"（左线必须在画面中心左侧），否则会把隔壁车道/横向纹理当选。

调参（不用改代码）：`--kp/--ki/--kd --limit-deg --target-ratio --roi-top/--roi-bottom-ratio
--band --pick steepest|longest --speed-us`。

## 3. 块 A：斑马线停车（先只放斑马线）

| # | 项目 | 命令 / 做法 | 通过标准 |
|---|---|---|---|
| 3.1 | 元素检测 | `car-mode.sh auto`（dry-run 即可）看日志里 `zebra` 的分数与框 | 1.4m 外就能稳定检出（连续 5 帧） |
| 3.2 | 距离估计标定 | 把车摆在距斑马线 1/2/3/5m，记录检测框**底边 y** | 拟合 `d = k/(y - y0)`，写进 `config/ground_map.yaml` |
| 3.3 | 减速停准 | 车从起点出发 | 停在**距横道边缘 30cm 内**、停满 10s、期间播报 |
| 3.4 | 重复性 | 连做 5 次 | 5/5 通过，记录每次实测距离 |

---

## 4. 块 B：锥桶绕行（S 走位）

| # | 项目 | 通过标准 |
|---|---|---|
| 4.1 | 锥桶检测 | 3m 外稳定检出 `cone`，左右各一个能分辨 |
| 4.2 | 开环 S 走位 | 固定舵角 + 时间/距离触发，连续 5 次不碰锥（碰一次 +20s） |
| 4.3 | 与块 A 串联 | 斑马线停完 → 继续走 → 绕锥 → 不压线 |

---

## 5. 块 C：停车入库（最后做）

| # | 项目 | 通过标准 |
|---|---|---|
| 5.1 | 蓝板判定停哪格 | 板在上格=停下面那格（反之亦然），5/5 判对 |
| 5.2 | 入库 | 四轮都在格内、不压黄线 |
| 5.3 | 端到端 | 发车→斑马线→锥桶→入库 全流程，记录总用时与每次罚时 |

---

## 6. 现场记录（每次调试后填，回来贴进本文件）

| 日期 | 场地 | 做了什么 | 结果 | 下一步 |
|---|---|---|---|---|
| 2026-09-19 | 操场（第一次） | ① 扫线视角/光照 ② 模型自检 ③ 直行基准 | | |
